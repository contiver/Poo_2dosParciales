public class Sets{
	public static <S,T> Map<S,T> setToMap(Set<T> set, Function<S,T> f){
		/* El resto es identico al setToMap del punto (a)
		 * con la excepción de que en la linea que dice:
		 * for(T element: this)
		 * se reemplaza el this por set dejando:
		 * for(T element: set)
		 */
	}
}

